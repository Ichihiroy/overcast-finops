# Desired state for backend in app-staging — reconciled by Argo CD (ADR-0008).
# CI's promote-staging job is the ONLY writer of image.tag; humans may edit
# other overrides via PR. Cluster identifiers (ACR, client ids, Key Vault) are
# injected by Terraform (infra/terraform/gitops.tf), never committed here.
image:
  tag: "sha-94496c9" # sha-<gitsha>, written by CI on every main build; empty until the first pipeline run
